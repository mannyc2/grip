pre-commit:
  jobs:
    - name: Web
      root: packages/web/
      parallel: true
      group:
        jobs:
          - name: typecheck
            run: pnpm typecheck
          - name: lint
            run: pnpm lint
